can_egg = false // no more!!
can_doublejump = true

music_set_slot_count(1)
music_set_slot_target(0,mu_nothing)
music_set_slot_target(1,mu_golf2)
music_set_active_slot(0)

window_set_caption("Pantry shootout in the " + gamename)