can_egg = false // no more!!
can_doublejump = true

music_set_slot_count(4)
music_set_slot_target(0,mu_garbage)
music_set_slot_target(1,mu_garbagesky)
music_set_slot_target(2,mu_car)
music_set_slot_target(3,mu_shootthebogos)
music_set_slot_target(4,mu_nothing)
music_set_active_slot(0)

window_set_caption(gamename + " is garbage.")