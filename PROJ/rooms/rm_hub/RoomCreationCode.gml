can_egg = false // no more!!
can_doublejump = true

music_set_slot_count(2)
music_set_slot_target(0,mu_hubtt2)
music_set_slot_target(1,mu_party)
music_set_slot_target(2,mu_danceroom)
music_set_active_slot(0)

window_set_caption("The chill zone of the " + gamename)